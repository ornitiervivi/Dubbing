# LIMITATIONS

## 2026-05-07
- Ambiente local desta execução não possui Docker CLI disponível, limitando validação de `docker compose config` e `docker build` localmente.
- O repositório ainda possui documentação extensa com placeholders fora do escopo de estabilização mínima realizada nesta tarefa.
